#ifndef __CONFIG_H
#define __CONFIG_H

/*============================================================================
 * 温度参数
 *============================================================================*/
#define DEFAULT_TARGET_TEMP     12.0f   /* 开机默认目标温度(℃) */
#define TEMP_MIN                16.0f   /* 蓝牙可设最低温度 */
#define TEMP_MAX                35.0f   /* 蓝牙可设最高温度 */
#define TEMP_ERROR             -99.0f   /* 温度传感器读取失败返回值 */

/*============================================================================
 * PID控制参数（影响自动模式下的风扇响应速度）
 *============================================================================*/
#define PID_KP                  16.0f   /* 比例系数：温差1℃ → 16%输出 */
#define PID_KI                   0.2f   /* 积分系数：消除稳态误差 */
#define PID_KD                   1.5f   /* 微分系数：抑制超调和震荡 */
#define PID_OUTPUT_MAX         100.0f   /* PID输出上限（风扇最大占空比%） */
#define PID_OUTPUT_MIN           0.0f   /* PID输出下限（风扇最小占空比%） */
#define PID_INTEGRAL_MAX        30.0f   /* 积分限幅：防止积分饱和过大 */
#define PID_DEAD_ZONE            0.1f   /* 死区范围(±℃)，范围内不调节风扇 */

/*============================================================================
 * 温度阈值（业务逻辑判断）
 *============================================================================*/
#define TEMP_DIFF_OFF_THRESHOLD -0.5f   /* 温差低于此值直接关闭风扇 */
#define TEMP_DIFF_RESUME        0.2f    /* 温差回升到此值恢复PID调节 */

/*============================================================================
 * LED闪烁参数（蓝牙模式未连接时使用）
 *============================================================================*/
#define LED_BLINK_PERIOD_MS     500     /* 闪烁周期：完整一次亮灭500ms */
#define LED_BLINK_HALF_MS       250     /* 半周期：亮250ms，灭250ms */

/*============================================================================
 * 手动档位PWM比较值（定时器ARR=1000，比较值=占空比×10）
 *============================================================================*/
#define GEAR_PWM_0              0       /* 0档：占空比0%，风扇停转 */
#define GEAR_PWM_1              250     /* 1档：占空比25% */
#define GEAR_PWM_2              500     /* 2档：占空比50% */
#define GEAR_PWM_3              750     /* 3档：占空比75% */
#define GEAR_PWM_4              1000    /* 4档：占空比100% */

/*============================================================================
 * 传感器故障安全保护值
 *============================================================================*/
#define ERROR_SAFE_GEAR         2       /* 传感器故障时强制使用的档位 */
#define ERROR_SAFE_DUTY         50.0f   /* 传感器故障时固定占空比50% */

#endif